#include<bits/stdc++.h>
using namespace std;

int main()
{
    int arr[3];
    cin>>arr[0]>>arr[1]>>arr[2];
    sort(arr,arr+3);
   int a=arr[2]-arr[0];
   int b=arr[2]-arr[1];
   int m=min(a,b);
   a-=0;
   b-=0;
   if(a==0)
   {
       if(b%)
   }
}
