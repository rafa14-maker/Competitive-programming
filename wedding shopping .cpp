#include<bits/stdc++.h>
using namespace std;

int main()
{
    int q;
    cin>>q;
    while(q--)
    {
        int n,m;
        cin>>m>>n;
       int arr[20][20];

       memset(arr,-1,sizeof arr);

       for(int i)
    }
}
