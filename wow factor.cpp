#include<bits/stdc++.h>
using namespace std;

int main()
{
    string arr;
    cin>>arr;
    int counter=0;
    for(int i=0;i<arr.size();i++)
    {
        if(arr[i]=='o'&&i-2>=0&&i+2<=arr.size()-1)
        {
            int flag=0;
            while()
        }
    }
}
