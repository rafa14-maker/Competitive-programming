#include<bits/stdc++.h>
using namespace std;

int main()
{
    int q;
    cin>>q;
    while(q--)
    {
        int n,k;
        cin>>n>>k;
       // int arr[n+5],frr[n+5];
      //  for(int i=0;i<n;i++)cin>>arr[i],frr[i]=arr[i];
    }
}
